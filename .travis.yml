dist: xenial

language: python
python:
  - '3.7'

services:
  - docker

install: true

script:
  - ./scripts/travis_tests.sh

after_script:
  - ./scripts/travis_tests_teardown.sh

after_failure:
  - ./scripts/travis_tests_failure.sh
