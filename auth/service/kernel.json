{
  "name": "kernel",
  "host": "http://kernel:8000",
  "paths": [
    "/{public_realm}/{name}$",
    "/{public_realm}/{name}/"
  ],
  "oidc_endpoints": [
    {
      "name": "protected",
      "paths": [
        "/{realm}/{name}$",
        "/{realm}/{name}/"
      ]
    }
  ]
}
